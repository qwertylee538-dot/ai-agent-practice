# Copy this file to ".env" (remove ".example" from the name) and paste
# your real API key after the "=" sign. The ".env" file is listed in
# .gitignore, so it will NEVER be uploaded to GitHub — this is the
# standard, safe way to keep secrets out of your source code.

DEEPSEEK_API_KEY=sk-paste-your-real-key-here
